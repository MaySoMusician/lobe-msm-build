From 05163031bc062dadbe4fb49c7b7fd1f86c35de50 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Tue, 31 Mar 2026 19:01:33 +0000
Subject: [PATCH 01/28] Add optional arg to set npm registry for Dockerfile

---
 Dockerfile | 4 ++++
 1 file changed, 4 insertions(+)

diff --git a/Dockerfile b/Dockerfile
index 92371587b9..562c9464fc 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -29,6 +29,7 @@ RUN set -e && \
 ## Builder image, install all the dependencies and build the app
 FROM base AS builder
 
+ARG NPM_REGISTRY=""
 ARG USE_CN_MIRROR
 ARG NEXT_PUBLIC_BASE_PATH
 ARG NEXT_PUBLIC_SENTRY_DSN
@@ -82,6 +83,9 @@ RUN set -e && \
         npm config set registry "https://registry.npmmirror.com/"; \
         echo 'canvas_binary_host_mirror=https://npmmirror.com/mirrors/canvas' >> .npmrc; \
     fi && \
+    if [ -n "${NPM_REGISTRY:-}" ]; then \
+        npm config set registry "${NPM_REGISTRY}"; \
+    fi && \
     export COREPACK_NPM_REGISTRY=$(npm config get registry | sed 's/\/$//') && \
     npm i -g corepack@latest && \
     corepack enable && \
-- 
2.39.5


From e98a90335c75f49b71f249de26441c2c5ad01915 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Mon, 6 Apr 2026 20:40:07 +0000
Subject: [PATCH 02/28] Set retry options for pnpm install inside Dockerfile

---
 Dockerfile | 3 +++
 1 file changed, 3 insertions(+)

diff --git a/Dockerfile b/Dockerfile
index 562c9464fc..39b234c8a3 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -90,6 +90,9 @@ RUN set -e && \
     npm i -g corepack@latest && \
     corepack enable && \
     corepack use $(sed -n 's/.*"packageManager": "\(.*\)".*/\1/p' package.json) && \
+    pnpm config set fetchRetries 5 && \
+    pnpm config set fetchRetryMintimeout 30000 && \
+    pnpm config set networkConcurrency 8 && \
     pnpm i && \
     mkdir -p /deps && \
     cd /deps && \
-- 
2.39.5


From 70895c9e9d747fef723694976cd7b7fc652b4400 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Tue, 12 May 2026 18:45:03 +0000
Subject: [PATCH 03/28] Harden npm (pnpm) security

---
 .npmrc              |  2 +-
 package.json        |  6 +++++-
 pnpm-workspace.yaml | 27 +++++++++++++++++++++++++--
 3 files changed, 31 insertions(+), 4 deletions(-)

diff --git a/.npmrc b/.npmrc
index 999be96503..836d41ee82 100644
--- a/.npmrc
+++ b/.npmrc
@@ -3,7 +3,7 @@ resolution-mode=highest
 dedupe-peer-dependents=true
 
 ignore-workspace-root-check=true
-enable-pre-post-scripts=true
+enable-pre-post-scripts=false
 
 
 public-hoist-pattern[]=*@umijs/lint*
diff --git a/package.json b/package.json
index b48b3707c6..7793a909a5 100644
--- a/package.json
+++ b/package.json
@@ -569,7 +569,11 @@
   "pnpm": {
     "onlyBuiltDependencies": [
       "@lobehub/editor",
-      "ffmpeg-static"
+      "ffmpeg-static",
+      "esbuild@0.18.20 || 0.25.12 || 0.27.7",
+      "protobufjs@7.5.6",
+      "sharp@0.34.5",
+      "unrs-resolver@1.11.1"
     ],
     "overrides": {
       "@react-pdf/image": "3.0.4",
diff --git a/pnpm-workspace.yaml b/pnpm-workspace.yaml
index 287c2fa2fa..5a4ad69203 100644
--- a/pnpm-workspace.yaml
+++ b/pnpm-workspace.yaml
@@ -7,12 +7,16 @@ packages:
   - apps/server
 
 minimumReleaseAgeExclude:
-  - '@lobehub/ui@5.20.2'
+  - '@lobehub/*'
 
 onlyBuiltDependencies:
   - '@google/genai'
   - '@lobehub/editor'
-  - '@vercel/speed-insights'
+
+ignoredBuiltDependencies:
+  - 'core-js'
+  - 'electron'
+  - 'es5-ext'
 
 overrides:
   jose: ^6.1.3
@@ -24,3 +28,22 @@ overrides:
 
 patchedDependencies:
   '@upstash/qstash': patches/@upstash__qstash.patch
+
+minimumReleaseAge: 10080
+minimumReleaseAgeStrict: true # Only works after v11.x
+
+trustPolicy: no-downgrade
+trustPolicyExclude:
+  - 'ua-parser-js@1.0.41'
+  - 'pdf-parse@1.1.4'
+  - 'undici-types@6.21.0'
+  - 'chokidar@4.0.3'
+  - 'semver@5.7.2 || 6.3.1'
+  - 'eslint-config-prettier@9.1.2'
+  - '@octokit/plugin-paginate-rest@9.2.2'
+  - '@octokit/endpoint@9.0.6'
+  - '@trickfilm400/rollup-plugin-off-main-thread@3.0.0-pre1'
+  - '@pierre/theme@1.1.0'
+blockExoticSubdeps: true
+strictDepBuilds: true
+enablePrePostScripts: false
-- 
2.39.5


From 76a26460cfd0c0bdd743e74606eeacbf55ed849d Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Wed, 13 May 2026 17:43:57 +0000
Subject: [PATCH 04/28] Harden Dockerfile security

---
 Dockerfile | 10 +++++++---
 1 file changed, 7 insertions(+), 3 deletions(-)

diff --git a/Dockerfile b/Dockerfile
index 39b234c8a3..2fd88d2fb6 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -87,17 +87,21 @@ RUN set -e && \
         npm config set registry "${NPM_REGISTRY}"; \
     fi && \
     export COREPACK_NPM_REGISTRY=$(npm config get registry | sed 's/\/$//') && \
-    npm i -g corepack@latest && \
     corepack enable && \
-    corepack use $(sed -n 's/.*"packageManager": "\(.*\)".*/\1/p' package.json) && \
     pnpm config set fetchRetries 5 && \
     pnpm config set fetchRetryMintimeout 30000 && \
     pnpm config set networkConcurrency 8 && \
+    pnpm config set minimumReleaseAge 10080 && \
+    pnpm config set minimumReleaseAgeStrict true && \
+    pnpm config set trustPolicy no-downgrade && \
+    pnpm config set blockExoticSubdeps true && \
+    pnpm config set strictDepBuilds true && \
+    pnpm config set enablePrePostScripts true && \
     pnpm i && \
     mkdir -p /deps && \
     cd /deps && \
     echo '{"name":"deps","private":true}' > package.json && \
-    pnpm add pg drizzle-orm
+    pnpm add -E pg@8.20.0 drizzle-orm@0.45.2
 
 COPY . .
 
-- 
2.39.5


From dad5eff1be32e0ba49063dd05d8402083fa70c16 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Fri, 10 Apr 2026 19:13:51 +0000
Subject: [PATCH 05/28] Dedupe @lobehub/ui package in vite

---
 vite.config.ts | 1 +
 1 file changed, 1 insertion(+)

diff --git a/vite.config.ts b/vite.config.ts
index 00362fd606..e888933efa 100644
--- a/vite.config.ts
+++ b/vite.config.ts
@@ -121,6 +121,7 @@ export default defineConfig({
     bundledDev: false,
   },
   resolve: {
+    dedupe: ['@lobehub/ui', '@lobehub/icons', 'antd', 'motion', 'react', 'react-dom'],
     tsconfigPaths: true,
   },
   optimizeDeps: sharedOptimizeDeps,
-- 
2.39.5


From 80a4f698b2811affde763cea6c7d146cb417f325 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Thu, 6 Feb 2025 20:34:05 +0000
Subject: [PATCH 06/28] Load Google Fonts script and IBM Plex Sans JP

---
 index.html        | 6 ++++++
 index.mobile.html | 6 ++++++
 2 files changed, 12 insertions(+)

diff --git a/index.html b/index.html
index 5c3972c99e..021623e67d 100644
--- a/index.html
+++ b/index.html
@@ -127,6 +127,12 @@
     <script>
       window.__SERVER_CONFIG__ = undefined; /* SERVER_CONFIG */
     </script>
+    <link href="https://fonts.googleapis.com" rel="preconnect" />
+    <link crossorigin="anonymous" href="https://fonts.gstatic.com" rel="preconnect" />
+    <link
+      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+JP:wght@400;500;600;700&display=swap"
+      rel="stylesheet"
+    />
   </head>
 
   <body>
diff --git a/index.mobile.html b/index.mobile.html
index 18d4ba16b6..e67ee4d463 100644
--- a/index.mobile.html
+++ b/index.mobile.html
@@ -62,6 +62,12 @@
     <script>
       window.__SERVER_CONFIG__ = undefined; /* SERVER_CONFIG */
     </script>
+    <link href="https://fonts.googleapis.com" rel="preconnect" />
+    <link crossorigin="anonymous" href="https://fonts.gstatic.com" rel="preconnect" />
+    <link
+      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+JP:wght@400;500;600;700&display=swap"
+      rel="stylesheet"
+    />
   </head>
 
   <body>
-- 
2.39.5


From d73b5ad5a089c5200f64738e6cb1e9b32c17fa65 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Fri, 7 Feb 2025 08:18:42 +0000
Subject: [PATCH 07/28] Force to use en-US as language

---
 src/utils/locale.ts | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/src/utils/locale.ts b/src/utils/locale.ts
index 5dbaf4408b..cdad3d1f89 100644
--- a/src/utils/locale.ts
+++ b/src/utils/locale.ts
@@ -52,9 +52,9 @@ export const parseBrowserLanguage = (headers: Headers, defaultLang: string = DEF
   );
 
   // if match the ar-EG then fallback to ar
-  if (browserLang === 'ar-EG') browserLang = 'ar';
+  if (browserLang === 'ar-EG') browserLang = 'ar'; // eslint-disable-line no-useless-assignment
 
-  return normalizeLocale(browserLang);
+  return 'en-US';
 };
 
 /**
-- 
2.39.5


From 97d3a920b76d8e4b9a3027d6ab9dbbffb062ca42 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Wed, 28 Jan 2026 01:28:48 +0000
Subject: [PATCH 08/28] Show only English and Japanese option for language
 options in global menu

---
 src/features/User/UserPanel/LangButton.tsx | 40 ++++++++++++----------
 1 file changed, 21 insertions(+), 19 deletions(-)

diff --git a/src/features/User/UserPanel/LangButton.tsx b/src/features/User/UserPanel/LangButton.tsx
index 6b301a2958..d696410d1b 100644
--- a/src/features/User/UserPanel/LangButton.tsx
+++ b/src/features/User/UserPanel/LangButton.tsx
@@ -40,25 +40,27 @@ const LangButton = memo<{ placement?: DropdownMenuProps['placement']; size?: num
         type: 'checkbox',
       };
 
-      const localeItems = localeOptions.map<DropdownMenuCheckboxItem>((item) => ({
-        checked: language === item.value,
-        closeOnClick: true,
-        key: item.value,
-        label: (
-          <Flexbox gap={4} key={item.value}>
-            <Text style={{ lineHeight: 1.2 }}>{item.label}</Text>
-            <Text fontSize={12} style={{ lineHeight: 1.2 }} type={'secondary'}>
-              {t(`lang.${item.value}` as any, { ns: 'common' })}
-            </Text>
-          </Flexbox>
-        ),
-        onCheckedChange: (checked: boolean) => {
-          if (checked) {
-            switchLocale(item.value);
-          }
-        },
-        type: 'checkbox',
-      }));
+      const localeItems = localeOptions
+        .filter((item) => ['en-US', 'ja-JP'].includes(item.value))
+        .map<DropdownMenuCheckboxItem>((item) => ({
+          checked: language === item.value,
+          closeOnClick: true,
+          key: item.value,
+          label: (
+            <Flexbox gap={4} key={item.value}>
+              <Text style={{ lineHeight: 1.2 }}>{item.label}</Text>
+              <Text fontSize={12} style={{ lineHeight: 1.2 }} type={'secondary'}>
+                {t(`lang.${item.value}` as any, { ns: 'common' })}
+              </Text>
+            </Flexbox>
+          ),
+          onCheckedChange: (checked: boolean) => {
+            if (checked) {
+              switchLocale(item.value);
+            }
+          },
+          type: 'checkbox',
+        }));
 
       return [autoItem, ...localeItems];
     }, [language, switchLocale, t]);
-- 
2.39.5


From 3ec9ae3e881ec7b2c9f8feac6ff8f24591a62ba7 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Wed, 8 Oct 2025 14:38:00 +0000
Subject: [PATCH 09/28] Overwrite reasoning.summary to undefined for requests
 for openai

---
 packages/model-runtime/src/providers/openai/index.ts | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/packages/model-runtime/src/providers/openai/index.ts b/packages/model-runtime/src/providers/openai/index.ts
index f9eb28415d..bce2e59a2e 100644
--- a/packages/model-runtime/src/providers/openai/index.ts
+++ b/packages/model-runtime/src/providers/openai/index.ts
@@ -93,8 +93,8 @@ export const params = {
 
       if (isOpenAIReasoningPayloadModel(model)) {
         const reasoning = payload.reasoning
-          ? { ...payload.reasoning, summary: 'auto' }
-          : { summary: 'auto' };
+          ? { ...payload.reasoning, summary: undefined }
+          : { summary: undefined };
         if (isGPT5ProResponsesModel(model)) {
           reasoning.effort = 'high';
         }
-- 
2.39.5


From 71be61a9f82dc432a42863354e0cfeced37f2ea8 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Wed, 28 Jan 2026 01:29:51 +0000
Subject: [PATCH 10/28] Disable streaming and allow Responses API for gpt-5
 family

---
 .../src/providers/openai/modelId.ts           | 60 +++++++++++++++++++
 1 file changed, 60 insertions(+)

diff --git a/packages/model-runtime/src/providers/openai/modelId.ts b/packages/model-runtime/src/providers/openai/modelId.ts
index a0811b1f27..17cebc8e55 100644
--- a/packages/model-runtime/src/providers/openai/modelId.ts
+++ b/packages/model-runtime/src/providers/openai/modelId.ts
@@ -34,6 +34,31 @@ export const disableStreamModels = new Set([
   */
   'computer-use-preview',
   'computer-use-preview-2025-03-11',
+  'gpt-5',
+  'gpt-5-2025-08-07',
+  'gpt-5-mini',
+  'gpt-5-mini-2025-08-07',
+  'gpt-5-pro',
+  'gpt-5-pro-2025-10-06',
+  'gpt-5.1',
+  'gpt-5.1-2025-11-13',
+  'gpt-5.2-pro-2025-12-11',
+  'gpt-5.2-pro',
+  'gpt-5.2',
+  'gpt-5.2-2025-12-11',
+  'gpt-5.4',
+  'gpt-5.4-2026-03-05',
+  'gpt-5.4-pro',
+  'gpt-5.4-pro-2026-03-05',
+  'gpt-5.5',
+  'gpt-5.5-2026-04-23',
+  'gpt-5.5-long-context',
+  'gpt-5.5-pro',
+  'gpt-5.5-pro-2026-04-23',
+  'gpt-5.5-pro-long-context',
+  'gpt-5.6-sol',
+  'gpt-5.6-terra',
+  'gpt-5.6-luna',
 ]);
 
 // Static Responses API-only exceptions that do not follow the parsed GPT-5 rule.
@@ -49,6 +74,41 @@ export const responsesAPIModels = new Set([
   'codex-mini-latest',
   'computer-use-preview',
   'computer-use-preview-2025-03-11',
+  'gpt-5-codex',
+  'gpt-5',
+  'gpt-5-2025-08-07',
+  'gpt-5-mini',
+  'gpt-5-mini-2025-08-07',
+  'gpt-5-nano',
+  'gpt-5-nano-2025-08-07',
+  'gpt-5-pro',
+  'gpt-5-pro-2025-10-06',
+  'gpt-5.1-codex',
+  'gpt-5.1-codex-mini',
+  'gpt-5.1-codex-max',
+  'gpt-5.1',
+  'gpt-5.1-2025-11-13',
+  'gpt-5.2',
+  'gpt-5.2-codex',
+  'gpt-5.2-pro-2025-12-11',
+  'gpt-5.2-pro',
+  'gpt-5.3-codex',
+  'gpt-5.2-2025-12-11',
+  'gpt-5.4',
+  'gpt-5.4-mini',
+  'gpt-5.4-nano',
+  'gpt-5.4-pro',
+  'gpt-5.4-2026-03-05',
+  'gpt-5.4-pro-2026-03-05',
+  'gpt-5.5',
+  'gpt-5.5-2026-04-23',
+  'gpt-5.5-long-context',
+  'gpt-5.5-pro',
+  'gpt-5.5-pro-2026-04-23',
+  'gpt-5.5-pro-long-context',
+  'gpt-5.6-sol',
+  'gpt-5.6-terra',
+  'gpt-5.6-luna',
 ]);
 
 const GPT_MODEL_PATTERN =
-- 
2.39.5


From 83e0ba104f100b1244a628e8ed08c00d5018579b Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Mon, 22 Sep 2025 15:02:20 +0000
Subject: [PATCH 11/28] Use stream flag value from handled payload

---
 .../model-runtime/src/core/openaiCompatibleFactory/index.ts     | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/packages/model-runtime/src/core/openaiCompatibleFactory/index.ts b/packages/model-runtime/src/core/openaiCompatibleFactory/index.ts
index 0aa33a65cd..b0c51e3304 100644
--- a/packages/model-runtime/src/core/openaiCompatibleFactory/index.ts
+++ b/packages/model-runtime/src/core/openaiCompatibleFactory/index.ts
@@ -1409,7 +1409,7 @@ export const createOpenAICompatibleRuntime = <T extends Record<string, any> = an
         strictToolPairing: true,
       });
 
-      const isStreaming = payload.stream !== false;
+      const isStreaming = res.stream !== false;
       log(
         'isStreaming: %s, hasTools: %s, hasReasoning: %s',
         isStreaming,
-- 
2.39.5


From c76faa894b204d288ac494c9e8a228f8aef905b0 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Mon, 22 Sep 2025 15:03:21 +0000
Subject: [PATCH 12/28] Introduce more detailed non-streaming response
 converter

e.g. handling url_citations
---
 .../nonStreamToStream.ts                      | 245 +++++++++++++++---
 1 file changed, 211 insertions(+), 34 deletions(-)

diff --git a/packages/model-runtime/src/core/openaiCompatibleFactory/nonStreamToStream.ts b/packages/model-runtime/src/core/openaiCompatibleFactory/nonStreamToStream.ts
index 2651c399c0..0bac5c17e1 100644
--- a/packages/model-runtime/src/core/openaiCompatibleFactory/nonStreamToStream.ts
+++ b/packages/model-runtime/src/core/openaiCompatibleFactory/nonStreamToStream.ts
@@ -89,47 +89,224 @@ export const transformResponseToStream = (data: OpenAI.ChatCompletion) =>
     },
   });
 
-/**
- * transform the OpenAI Response API data to stream format for non-streaming responses
- */
-export const transformResponseAPIToStream = (data: OpenAI.Responses.Response) =>
-  new ReadableStream({
-    start(controller) {
-      // Check if output exists and is an array
-      if (data.output && Array.isArray(data.output)) {
-        data.output.forEach((output) => {
-          switch (output.type) {
-            case 'message': {
-              // Check if content exists and is an array
-              if (output.content && Array.isArray(output.content)) {
-                output.content.forEach((content) => {
-                  switch (content.type) {
-                    case 'output_text': {
-                      // Only emit delta if text exists
-                      if (content.text) {
-                        controller.enqueue({
-                          delta: content.text,
-                          type: 'response.output_text.delta',
-                        });
-                      }
-                      break;
-                    }
-                  }
-                });
+function convertNonStreamingResponseToStreamEvents(
+  nonStreamingResponse: OpenAI.Responses.Response,
+): OpenAI.Responses.ResponseStreamEvent[] {
+  const events: OpenAI.Responses.ResponseStreamEvent[] = [];
+  let seq = 1;
+
+  // 1. First, create the "response.created" event.
+  events.push({
+    response: {
+      created_at: 0, // dummy placeholder
+      error: null, // dummy placeholder
+      id: nonStreamingResponse.id,
+      incomplete_details: null, // dummy placeholder
+      instructions: null, // dummy placeholder
+      metadata: null, // dummy placeholder
+      model: '', // dummy placeholder
+      object: 'response', // dummy placeholder
+      output: [], // dummy placeholder
+      output_text: '', // dummy placeholder
+      parallel_tool_calls: false, // dummy placeholder
+      status:
+        nonStreamingResponse.status === 'completed' ? 'in_progress' : nonStreamingResponse.status,
+      temperature: null, // dummy placeholder
+      tool_choice: 'auto', // dummy placeholder
+      tools: [], // dummy placeholder
+      top_p: null, // dummy placeholder
+      usage: undefined,
+    },
+    sequence_number: seq++,
+    type: 'response.created',
+  });
+
+  // 2. Traverse the output array and generate events according to each item's type.
+  for (const item of nonStreamingResponse.output) {
+    // Send the "response.output_item.added" event first.
+    events.push({
+      item: {
+        arguments: (item as any).arguments,
+        call_id: (item as any).call_id,
+        content: (item as any).content,
+        id: item.id,
+        name: (item as any).name,
+        role: (item as any).role,
+        status: (item as any).status,
+        summary: (item as any).summary,
+        type: item.type,
+      },
+      sequence_number: seq++,
+      type: 'response.output_item.added',
+    } as any);
+
+    // Branch processing according to item.type.
+    switch (item.type) {
+      case 'function_call': {
+        // In src/utils/fetch/fetchSSE.ts, the argument duplication occurs during parse, so we won't send a delta.
+        events.push({
+          item: {
+            arguments: item.arguments, // dummy placeholder
+            call_id: item.call_id,
+            id: item.id,
+            name: '', // dummy placeholder
+            type: 'function_call',
+          },
+          output_index: 1,
+          sequence_number: seq++,
+          type: 'response.output_item.done',
+        });
+        break;
+      }
+
+      case 'message': {
+        // For "message" type, send the entire content as a single "response.output_text.delta" event.
+        if (item.content) {
+          for (const contentObj of item.content) {
+            if (contentObj.type === 'output_text') {
+              // Combine text into a single delta.
+              events.push({
+                content_index: 1, // dummy placeholder
+                delta: contentObj.text,
+                item_id: item.id,
+                output_index: 1, // dummy placeholder
+                sequence_number: seq++,
+                type: 'response.output_text.delta',
+                logprobs: [], // dummy placeholder
+              });
+
+              // If there are annotations, send them individually as "response.output_text.annotation.added" events.
+              if (Array.isArray(contentObj.annotations)) {
+                for (const ann of contentObj.annotations) {
+                  const ann2 = ann as OpenAI.Responses.ResponseOutputText.URLCitation;
+                  events.push({
+                    annotation: {
+                      end_index: ann2.end_index,
+                      start_index: ann2.start_index,
+                      title: ann2.title,
+                      type: 'url_citation',
+                      url: ann2.url,
+                    },
+                    item_id: item.id,
+                    sequence_number: seq++,
+                    type: 'response.output_text.annotation.added' as any,
+                  });
+                }
               }
-              break;
             }
           }
+        }
+
+        // Finally, send "response.output_item.done".
+        events.push({
+          item: {
+            content: [], // dummy placeholder
+            id: item.id,
+            role: 'assistant', // dummy placeholder
+            status: 'in_progress', // dummy placeholder
+            type: 'message',
+          },
+          output_index: 1, // dummy placeholder
+          sequence_number: seq++,
+          type: 'response.output_item.done',
         });
+        break;
       }
 
-      // Always send response.completed event
-      controller.enqueue({
-        response: data,
-        sequence_number: 999,
-        type: 'response.completed',
-      } as OpenAI.Responses.ResponseStreamEvent);
+      case 'reasoning': {
+        // For "reasoning" type, send the summary all at once.
+        // (In transformOpenAIStream, the events go in the order "response.reasoning_summary_part.added" -> "response.reasoning_summary_text.delta" -> done.)
+        events.push({
+          item_id: item.id,
+          output_index: 1, // dummy placeholder
+          part: {
+            text: '', // dummy placeholder
+            type: 'summary_text',
+          },
+          sequence_number: seq++,
+          summary_index: 1, // dummy placeholder
+          type: 'response.reasoning_summary_part.added',
+        });
 
+        // If there's a summary array, concatenate it into a single delta.
+        const entireSummary = Array.isArray(item.summary) ? item.summary.join('\n') : '';
+        if (entireSummary) {
+          events.push({
+            delta: entireSummary,
+            item_id: item.id,
+            output_index: 1, // dummy placeholder
+            sequence_number: seq++,
+            summary_index: 1, // dummy placeholder
+            type: 'response.reasoning_summary_text.delta',
+          });
+        }
+
+        events.push({
+          item: {
+            id: item.id,
+            summary: [], // dummy placeholder
+            type: 'reasoning',
+          },
+          output_index: 1, // dummy placeholder
+          sequence_number: seq++,
+          type: 'response.output_item.done',
+        });
+        break;
+      }
+
+      default: {
+        // For an unknown type, just send "response.output_item.done".
+        events.push({
+          item: {
+            id: item.id,
+            type: item.type,
+          },
+          sequence_number: seq++,
+          type: 'response.output_item.done',
+        } as any);
+        break;
+      }
+    }
+  }
+
+  // 3. Finally, send the "response.completed" event.
+  events.push({
+    response: {
+      created_at: 0, // dummy placeholder
+      error: null, // dummy placeholder
+      id: nonStreamingResponse.id,
+      incomplete_details: null, // dummy placeholder
+      instructions: null, // dummy placeholder
+      metadata: null, // dummy placeholder
+      model: '', // dummy placeholder
+      object: 'response', // dummy placeholder
+      output: [], // dummy placeholder
+      output_text: '', // dummy placeholder
+      parallel_tool_calls: false, // dummy placeholder
+      temperature: null, // dummy placeholder
+      tool_choice: 'auto', // dummy placeholder
+      tools: [], // dummy placeholder
+      top_p: null, // dummy placeholder
+      usage: nonStreamingResponse.usage ?? undefined,
+    },
+    sequence_number: seq++,
+    type: 'response.completed',
+  });
+
+  return events;
+}
+
+/**
+ * transform the OpenAI Response API data to stream format for non-streaming responses
+ */
+export const transformResponseAPIToStream = (data: OpenAI.Responses.Response) =>
+  new ReadableStream({
+    start(controller) {
+      const events = convertNonStreamingResponseToStreamEvents(data);
+
+      for (const event of events) {
+        controller.enqueue(event);
+      }
       controller.close();
     },
   });
-- 
2.39.5


From 4aaaff013348ac534c405c396bbbd5d200d4747a Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Sun, 12 Oct 2025 16:37:16 +0000
Subject: [PATCH 13/28] Use conversation language for summaryTitle

---
 packages/prompts/src/chains/summaryTitle.ts | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/packages/prompts/src/chains/summaryTitle.ts b/packages/prompts/src/chains/summaryTitle.ts
index b8d5034d1c..d627cabbf3 100644
--- a/packages/prompts/src/chains/summaryTitle.ts
+++ b/packages/prompts/src/chains/summaryTitle.ts
@@ -18,7 +18,7 @@ Rules:
 - Maximum 15 words
 - Maximum 80 characters
 - No punctuation marks
-- Use the language specified by the locale code: ${locale}
+- Use the language mainly used in the conversation. If ambiguous, use "${locale}".
 - The title should accurately reflect the main topic of the conversation
 - Keep it short and to the point`,
         role: 'system',
-- 
2.39.5


From 33c8eadd83b934147452f97ad4eed35ac8ee0daa Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Thu, 5 Mar 2026 10:44:37 +0000
Subject: [PATCH 14/28] Do not load default tools like lobe-tools etc.

Also show them in the skill list
---
 packages/builtin-tools/src/index.ts | 13 ++++---------
 1 file changed, 4 insertions(+), 9 deletions(-)

diff --git a/packages/builtin-tools/src/index.ts b/packages/builtin-tools/src/index.ts
index 33a0937327..871c88d6fe 100644
--- a/packages/builtin-tools/src/index.ts
+++ b/packages/builtin-tools/src/index.ts
@@ -71,12 +71,7 @@ export const defaultToolIds = [
  * They default to pinned but can be explicitly disabled per agent; entries represented by
  * the activation mode control itself are excluded from that menu.
  */
-export const alwaysOnToolIds = [
-  LobeAgentManifest.identifier,
-  LobeActivatorManifest.identifier,
-  SkillsManifest.identifier,
-  SkillStoreManifest.identifier,
-];
+export const alwaysOnToolIds = [];
 
 /**
  * Runtime tools represented by the skill activation mode control itself. They remain part
@@ -161,21 +156,21 @@ export const runtimeManagedToolIds = [
 const builtinToolRegistry: LobeBuiltinTool[] = [
   {
     discoverable: false,
-    hidden: true,
+    hidden: false,
     identifier: VerifyToolManifest.identifier,
     manifest: VerifyToolManifest,
     type: 'builtin',
   },
   {
     discoverable: false,
-    hidden: true,
+    hidden: false,
     identifier: LobeActivatorManifest.identifier,
     manifest: LobeActivatorManifest,
     type: 'builtin',
   },
   {
     discoverable: false,
-    hidden: true,
+    hidden: false,
     identifier: SkillsManifest.identifier,
     manifest: SkillsManifest,
     // Context-aware: prefixes exec-class API descriptions with the run's
-- 
2.39.5


From 60fae866faecb4ecd6942e17dfdffb8dc7d8f475 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Fri, 20 Mar 2026 18:26:33 +0000
Subject: [PATCH 15/28] Disable Markdown Editor for user messages

Until the editor can edit as text and save as markdown
---
 .../User/components/MessageContent.tsx        | 18 +++++----------
 src/features/EditorModal/EditorCanvas.tsx     | 23 ++++++++++++++++---
 2 files changed, 26 insertions(+), 15 deletions(-)

diff --git a/src/features/Conversation/Messages/User/components/MessageContent.tsx b/src/features/Conversation/Messages/User/components/MessageContent.tsx
index ccb4071193..c43528c741 100644
--- a/src/features/Conversation/Messages/User/components/MessageContent.tsx
+++ b/src/features/Conversation/Messages/User/components/MessageContent.tsx
@@ -11,30 +11,24 @@ import AudioFileListViewer from './AudioFileListViewer';
 import FileListViewer from './FileListViewer';
 import ImageFileListViewer from './ImageFileListViewer';
 import PageSelections from './PageSelections';
-import RichTextMessage from './RichTextMessage';
 import VideoFileListViewer from './VideoFileListViewer';
 
 const UserMessageContent = memo<UIChatMessage>(
-  ({ id, content, editorData, imageList, videoList, audioList, fileList, metadata }) => {
+  ({ id, content, imageList, videoList, audioList, fileList, metadata }) => {
     const markdownProps = useMarkdown(id);
     const selections = metadata?.contextSelections?.length
       ? metadata.contextSelections
       : metadata?.pageSelections;
     const displayContent = useMemo(() => (content ? cleanSpeakerTag(content) : content), [content]);
 
-    const hasEditorData =
-      editorData && typeof editorData === 'object' && Object.keys(editorData).length > 0;
-
-    const textBody = hasEditorData ? (
-      <RichTextMessage editorState={editorData} />
-    ) : (
-      displayContent && <MarkdownMessage {...markdownProps}>{displayContent}</MarkdownMessage>
-    );
-
     return (
       <Flexbox gap={8} id={id}>
         {selections && selections.length > 0 && <PageSelections selections={selections} />}
-        {textBody && <CollapsibleContent>{textBody}</CollapsibleContent>}
+        {displayContent && (
+          <CollapsibleContent>
+            <MarkdownMessage {...markdownProps}>{displayContent}</MarkdownMessage>
+          </CollapsibleContent>
+        )}
         {imageList && imageList?.length > 0 && <ImageFileListViewer items={imageList} />}
         {videoList && videoList?.length > 0 && <VideoFileListViewer items={videoList} />}
         {audioList && audioList?.length > 0 && <AudioFileListViewer items={audioList} />}
diff --git a/src/features/EditorModal/EditorCanvas.tsx b/src/features/EditorModal/EditorCanvas.tsx
index 8bf7cf234d..e99c6246b2 100644
--- a/src/features/EditorModal/EditorCanvas.tsx
+++ b/src/features/EditorModal/EditorCanvas.tsx
@@ -5,6 +5,8 @@ import { Flexbox } from '@lobehub/ui';
 import { type FC, useMemo } from 'react';
 
 import { createChatInputRichPlugins } from '@/features/ChatInput/InputEditor/plugins';
+import { useUserStore } from '@/store/user';
+import { labPreferSelectors } from '@/store/user/selectors';
 
 import TypoBar from './Typobar';
 
@@ -28,12 +30,27 @@ const EditorCanvas: FC<EditorCanvasProps> = ({ defaultValue, editor, editorData
       return { content: JSON.stringify(editorData), type: 'json' as const };
     }
 
-    return { content: defaultValue || '', type: 'markdown' as const };
+    return { content: defaultValue || '', type: 'text' as const };
   }, [editorData, defaultValue]);
 
+  const enableRichRender = useUserStore(labPreferSelectors.enableInputMarkdown);
+  const richRenderProps = useMemo(
+    () =>
+      !enableRichRender
+        ? {
+            enablePasteMarkdown: false,
+            markdownOption: false,
+            plugins: [],
+          }
+        : {
+            plugins: EDITOR_PLUGINS,
+          },
+    [enableRichRender],
+  );
+
   return (
     <>
-      <TypoBar editor={editor} />
+      {enableRichRender && <TypoBar editor={editor} />}
       <Flexbox
         padding={16}
         style={{ cursor: 'text', maxHeight: '80vh', minHeight: '50vh', overflowY: 'auto' }}
@@ -42,7 +59,7 @@ const EditorCanvas: FC<EditorCanvasProps> = ({ defaultValue, editor, editorData
           autoFocus
           content={content}
           editor={editor}
-          plugins={EDITOR_PLUGINS}
+          {...richRenderProps}
           type={type}
           variant={'chat'}
           style={{
-- 
2.39.5


From a742a9762da428582def10e28b6c3354b1e326b1 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Sun, 29 Mar 2026 12:25:13 +0000
Subject: [PATCH 16/28] Always show exact token number

---
 packages/utils/src/format.ts                         | 3 ---
 src/features/Conversation/ChatInput/OpStatusTray.tsx | 4 +---
 2 files changed, 1 insertion(+), 6 deletions(-)

diff --git a/packages/utils/src/format.ts b/packages/utils/src/format.ts
index e8d929ebc7..a67591f76c 100644
--- a/packages/utils/src/format.ts
+++ b/packages/utils/src/format.ts
@@ -82,9 +82,6 @@ export const formatIntergerNumber = (num?: any) => {
 };
 
 export const formatUsageValue = (number: number) => {
-  if (number >= 1_000_000_000) return `${numeral(number / 1_000_000_000).format('0.[0]')}B`;
-  if (number >= 1_000_000) return `${numeral(number / 1_000_000).format('0.[0]')}M`;
-  if (number >= 1_000) return `${numeral(number / 1_000).format('0.[0]')}K`;
   return numeral(number).format('0,0');
 };
 
diff --git a/src/features/Conversation/ChatInput/OpStatusTray.tsx b/src/features/Conversation/ChatInput/OpStatusTray.tsx
index 2c19029d22..33294cd784 100644
--- a/src/features/Conversation/ChatInput/OpStatusTray.tsx
+++ b/src/features/Conversation/ChatInput/OpStatusTray.tsx
@@ -208,9 +208,7 @@ const ActivityGlyph = memo(() => (
 ActivityGlyph.displayName = 'ActivityGlyph';
 
 const formatTokens = (n: number) => {
-  if (n < 1000) return String(n);
-  if (n < 1_000_000) return `${(n / 1000).toFixed(1)}k`;
-  return `${(n / 1_000_000).toFixed(2)}M`;
+  return String(n);
 };
 
 const formatCost = (cost: number) => {
-- 
2.39.5


From 7b97a8bb3cb242144ce8e804d5a0d1148d08c5ae Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Mon, 27 Apr 2026 06:51:48 +0000
Subject: [PATCH 17/28] Open reasoning & verbosity config first, instead of
 pricing card

---
 src/store/global/initialState.ts | 3 ---
 1 file changed, 3 deletions(-)

diff --git a/src/store/global/initialState.ts b/src/store/global/initialState.ts
index ca4aec9545..fc2e3116cd 100644
--- a/src/store/global/initialState.ts
+++ b/src/store/global/initialState.ts
@@ -130,9 +130,6 @@ export type ModelDetailPanelExpandedKey = (typeof MODEL_DETAIL_PANEL_EXPANDED_KE
  * newly added sections default to expanded automatically.
  */
 export const MODEL_DETAIL_PANEL_EXPANDABLE_KEYS = [
-  'rating',
-  'abilities',
-  'pricing',
   'config',
 ] as const satisfies readonly ModelDetailPanelExpandedKey[];
 
-- 
2.39.5


From 9ce3f17f7b759b2673ebc97ed513edf2f42df91b Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Mon, 27 Apr 2026 14:37:03 +0000
Subject: [PATCH 18/28] Don't resend the edited message automatically

It would be nice for many people, but I want to just rather save it.
---
 .../ChatItem/components/MessageContent/index.tsx    | 13 +++----------
 1 file changed, 3 insertions(+), 10 deletions(-)

diff --git a/src/features/Conversation/ChatItem/components/MessageContent/index.tsx b/src/features/Conversation/ChatItem/components/MessageContent/index.tsx
index 5c18506d89..ba01d302d4 100644
--- a/src/features/Conversation/ChatItem/components/MessageContent/index.tsx
+++ b/src/features/Conversation/ChatItem/components/MessageContent/index.tsx
@@ -4,11 +4,7 @@ import { type ReactNode } from 'react';
 import { memo, Suspense, useCallback } from 'react';
 import { useTranslation } from 'react-i18next';
 
-import {
-  dataSelectors,
-  messageStateSelectors,
-  useConversationStore,
-} from '@/features/Conversation/store';
+import { dataSelectors, useConversationStore } from '@/features/Conversation/store';
 import { usePermission } from '@/hooks/usePermission';
 import dynamic from '@/libs/next/dynamic';
 
@@ -82,11 +78,8 @@ const MessageContent = memo<MessageContentProps>(
     // otherwise the initial send phase — where the persisted id has just swapped in
     // under an optimistic tmp_* op — would flip to Send and kick off a duplicate
     // regenerate for the same prompt.
-    const shouldSendOnConfirm = useConversationStore((s) => {
-      if (!editing) return false;
-      if (dataSelectors.getDisplayMessageById(id)(s)?.role !== 'user') return false;
-      if (s.displayMessages.findLast((m) => m.role === 'user')?.id !== id) return false;
-      return !messageStateSelectors.isInputLoading(s);
+    const shouldSendOnConfirm = useConversationStore(() => {
+      return false;
     });
 
     const { t } = useTranslation('common');
-- 
2.39.5


From 5fbf19ab8620b438e44272f3e040d45d340fea3d Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Fri, 19 Jun 2026 14:53:55 +0000
Subject: [PATCH 19/28] Pin docker base image

Node 24.17 has the issue of https://github.com/nodejs/node/issues/63989,
so pin the image to 24.16.0 temporarily
---
 Dockerfile | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/Dockerfile b/Dockerfile
index 2fd88d2fb6..e4692db592 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -1,5 +1,5 @@
 ## Set global build ENV
-ARG NODEJS_VERSION="24"
+ARG NODEJS_VERSION="24.16.0"
 
 ## Base image for all building stages
 FROM node:${NODEJS_VERSION}-slim AS base
@@ -113,7 +113,7 @@ RUN rm -rf src/app/desktop "src/app/(backend)/trpc/desktop"
 RUN npm run build:docker
 
 ## Application image, copy all the files for production
-FROM busybox:latest AS app
+FROM busybox:1.38.0 AS app
 
 COPY --from=base /distroless/ /
 
-- 
2.39.5


From 6f24823c60019a1dda8369f800ebb449e33c8b1d Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Tue, 23 Jun 2026 23:44:22 +0000
Subject: [PATCH 20/28] Open Share modal by defailt in OpenAI compatible JSON
 mode

---
 src/features/ShareModal/Modal.tsx           | 2 +-
 src/features/ShareModal/ShareJSON/index.tsx | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/src/features/ShareModal/Modal.tsx b/src/features/ShareModal/Modal.tsx
index fbd08e4533..2e140aaba0 100644
--- a/src/features/ShareModal/Modal.tsx
+++ b/src/features/ShareModal/Modal.tsx
@@ -28,7 +28,7 @@ export interface OpenShareModalOptions {
 }
 
 const ShareModalContent = memo(() => {
-  const [tab, setTab] = useState<Tab>(Tab.Screenshot);
+  const [tab, setTab] = useState<Tab>(Tab.JSON);
   const { t } = useTranslation('chat');
   const isMobile = useIsMobile();
   const { dbMessages, isLoading } = useShareData();
diff --git a/src/features/ShareModal/ShareJSON/index.tsx b/src/features/ShareModal/ShareJSON/index.tsx
index b76539e738..8d62db15a7 100644
--- a/src/features/ShareModal/ShareJSON/index.tsx
+++ b/src/features/ShareModal/ShareJSON/index.tsx
@@ -19,7 +19,7 @@ import Preview from './Preview';
 import { type FieldType } from './type';
 
 const DEFAULT_FIELD_VALUE: FieldType = {
-  exportMode: 'full',
+  exportMode: 'simple',
   includeTool: true,
   withSystemRole: true,
 };
-- 
2.39.5


From 9d39d913ce2fdb2d825e33f993561c7bda0d53cd Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Mon, 29 Jun 2026 14:03:03 +0000
Subject: [PATCH 21/28] Reset model switch panel width when opening it in
 narrower phone

---
 .../ModelSwitchPanel/components/PanelContent.tsx       | 10 +++++++++-
 1 file changed, 9 insertions(+), 1 deletion(-)

diff --git a/src/features/ModelSwitchPanel/components/PanelContent.tsx b/src/features/ModelSwitchPanel/components/PanelContent.tsx
index 3212079217..d47451004c 100644
--- a/src/features/ModelSwitchPanel/components/PanelContent.tsx
+++ b/src/features/ModelSwitchPanel/components/PanelContent.tsx
@@ -1,5 +1,5 @@
 import { Flexbox } from '@lobehub/ui';
-import { type ComponentType, type FC } from 'react';
+import { type ComponentType, type FC, useEffect } from 'react';
 import { useState } from 'react';
 import { Rnd } from 'react-rnd';
 
@@ -67,6 +67,14 @@ export const PanelContent: FC<PanelContentProps> = ({
     </>
   );
 
+  // Reset the panel width for narrower browser
+  useEffect(() => {
+    const clevererWidth = document.body.clientWidth - 50;
+    if (panelWidth > clevererWidth) {
+      handlePanelWidthChange(clevererWidth);
+    }
+  }, [handlePanelWidthChange]);
+
   if (isDevMode) {
     return (
       <Rnd
-- 
2.39.5


From a157d8fe0bcbace164ba67f56be62c89743fe61b Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Mon, 29 Jun 2026 18:42:37 +0000
Subject: [PATCH 22/28] Show right panel controls even in mobile (in not clean
 way)

---
 .../ChatInput/ActionBar/Params/Controls.tsx   |  1 -
 src/routes/(mobile)/chat/_layout/index.tsx    | 58 +++++++++++++++++--
 .../chat/features/ChatHeader/index.tsx        |  8 ++-
 3 files changed, 59 insertions(+), 8 deletions(-)

diff --git a/src/features/ChatInput/ActionBar/Params/Controls.tsx b/src/features/ChatInput/ActionBar/Params/Controls.tsx
index ea613bd0dd..09a2c2f9b6 100644
--- a/src/features/ChatInput/ActionBar/Params/Controls.tsx
+++ b/src/features/ChatInput/ActionBar/Params/Controls.tsx
@@ -101,7 +101,6 @@ const styles = createStaticStyles(({ css }) => ({
   `,
   body: css`
     overflow-y: auto;
-    overscroll-behavior: contain;
     display: flex;
     flex: 1 1 auto;
     flex-direction: column;
diff --git a/src/routes/(mobile)/chat/_layout/index.tsx b/src/routes/(mobile)/chat/_layout/index.tsx
index 0429a0a745..a3ef627e27 100644
--- a/src/routes/(mobile)/chat/_layout/index.tsx
+++ b/src/routes/(mobile)/chat/_layout/index.tsx
@@ -7,7 +7,10 @@ import MobileContentLayout from '@/components/server/MobileNavLayout';
 import { AgentNotFoundGuard } from '@/features/AgentNotFound';
 import { useInitAgentConfig } from '@/hooks/useInitAgentConfig';
 import AgentIdSync from '@/routes/(main)/agent/_layout/AgentIdSync';
+import AgentWorkingSidebar from '@/routes/(main)/agent/features/Conversation/WorkingSidebar';
 import ChatHeader from '@/routes/(mobile)/chat/features/ChatHeader';
+import { useGlobalStore } from '@/store/global';
+import { systemStatusSelectors } from '@/store/global/selectors';
 
 import { styles } from './style';
 
@@ -16,16 +19,59 @@ const Layout: FC = () => {
 
   return (
     <>
-      <MobileContentLayout className={styles.mainContainer} header={<ChatHeader />}>
-        {/* Same 404 fallback as the desktop agent layout: only the content
+      <div style={{ position: 'relative', height: '100%' }}>
+        <MobileContentLayout className={styles.mainContainer} header={<ChatHeader />}>
+          {/* Same 404 fallback as the desktop agent layout: only the content
             area collapses when the routed agent is gone or turned private. */}
-        <AgentNotFoundGuard>
-          <Outlet />
-        </AgentNotFoundGuard>
-      </MobileContentLayout>
+          <AgentNotFoundGuard>
+            <Outlet />
+          </AgentNotFoundGuard>
+        </MobileContentLayout>
+        <Overlay />
+      </div>
+      <div
+        style={{
+          position: 'absolute',
+          right: 0,
+          overflowY: 'auto',
+          maxHeight: '100vh',
+          zIndex: 999999,
+        }}
+      >
+        <AgentWorkingSidebar />
+      </div>
       <AgentIdSync />
     </>
   );
 };
 
+const Overlay = () => {
+  const [sidebarShown, toggleRightPanel] = useGlobalStore((s) => [
+    systemStatusSelectors.showRightPanel(s),
+    s.toggleRightPanel,
+  ]);
+  return (
+    sidebarShown && (
+      <div
+        style={{
+          position: 'absolute',
+          top: 0,
+          left: 0,
+          width: '100%',
+          height: '100%',
+          background: 'black',
+          opacity: 0.8,
+        }}
+        onClickCapture={(e) => {
+          if (sidebarShown) {
+            e.preventDefault();
+            toggleRightPanel();
+            e.stopPropagation();
+          }
+        }}
+      />
+    )
+  );
+};
+
 export default Layout;
diff --git a/src/routes/(mobile)/chat/features/ChatHeader/index.tsx b/src/routes/(mobile)/chat/features/ChatHeader/index.tsx
index a05d4a884c..3c60ee8f58 100644
--- a/src/routes/(mobile)/chat/features/ChatHeader/index.tsx
+++ b/src/routes/(mobile)/chat/features/ChatHeader/index.tsx
@@ -5,6 +5,7 @@ import { memo, useState } from 'react';
 
 import { useQueryRoute } from '@/hooks/useQueryRoute';
 import ShareButton from '@/routes/(main)/agent/features/Conversation/Header/ShareButton';
+import WorkingPanelToggle from '@/routes/(main)/agent/features/Conversation/Header/WorkingPanelToggle';
 
 import ChatHeaderTitle from './ChatHeaderTitle';
 
@@ -16,8 +17,13 @@ const MobileHeader = memo(() => {
     <ChatHeader
       showBackButton
       center={<ChatHeaderTitle />}
-      right={<ShareButton mobile open={open} setOpen={setOpen} />}
       style={{ width: '100%' }}
+      right={
+        <>
+          <ShareButton mobile open={open} setOpen={setOpen} />
+          <WorkingPanelToggle />
+        </>
+      }
       onBackClick={() =>
         // `/agent` index redirects to `..` (mobile home / session list), preserving
         // workspace scope; the old `?session=` query was never read by the target.
-- 
2.39.5


From 322659d6522b2d479104d15027892c99df08bd02 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Mon, 6 Jul 2026 06:42:46 +0000
Subject: [PATCH 23/28] Shorten message in topic card for faster performance

---
 src/features/AgentTopicManager/TopicCard.tsx | 13 ++++++++++---
 1 file changed, 10 insertions(+), 3 deletions(-)

diff --git a/src/features/AgentTopicManager/TopicCard.tsx b/src/features/AgentTopicManager/TopicCard.tsx
index 79918b2b18..d114e63db0 100644
--- a/src/features/AgentTopicManager/TopicCard.tsx
+++ b/src/features/AgentTopicManager/TopicCard.tsx
@@ -5,7 +5,7 @@ import { formatPrice, formatTokenNumber } from '@lobechat/utils/format';
 import { Block, Checkbox, Flexbox, Icon, Tag, Text } from '@lobehub/ui';
 import { createStaticStyles, cssVar } from 'antd-style';
 import { CircleDollarSign, FolderIcon, MessageSquare, Star, Zap } from 'lucide-react';
-import { memo, type MouseEvent, useCallback } from 'react';
+import { memo, type MouseEvent, useCallback, useMemo } from 'react';
 import { useTranslation } from 'react-i18next';
 
 import { useWorkspaceAwareNavigate } from '@/features/Workspace/useWorkspaceAwareNavigate';
@@ -118,8 +118,15 @@ const TopicCard = memo<TopicCardProps>(({ topic, agentId }) => {
   const status = topic.status ?? 'active';
   // Preview priority: user-written description → AI history summary → first user
   // message (sliced server-side when neither richer field exists).
-  const preview =
-    topic.description?.trim() || topic.historySummary?.trim() || topic.firstUserMessage?.trim();
+  const preview = useMemo(
+    () =>
+      (
+        topic.description?.trim() ||
+        topic.historySummary?.trim() ||
+        topic.firstUserMessage?.trim()
+      )?.slice(0, 100),
+    [topic.description, topic.firstUserMessage, topic.historySummary],
+  );
   const updatedAt = useActivityTime(topic.updatedAt);
   // Postgres `numeric` / `int` round-trip through TRPC/JSON as strings in
   // some shapes, so coerce defensively before any `.toFixed` / format call.
-- 
2.39.5


From a7b4b29d9fc314af96e915562e1a0ece3321a01f Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Mon, 6 Jul 2026 07:40:48 +0000
Subject: [PATCH 24/28] Allow to open topic in new tab via Ctrl+Click

---
 src/features/AgentTopicManager/TopicCard.tsx  | 20 ++++++++++++-
 .../_layout/Sidebar/Topic/List/Item/index.tsx | 29 +++++++++++--------
 2 files changed, 36 insertions(+), 13 deletions(-)

diff --git a/src/features/AgentTopicManager/TopicCard.tsx b/src/features/AgentTopicManager/TopicCard.tsx
index d114e63db0..a3d3640966 100644
--- a/src/features/AgentTopicManager/TopicCard.tsx
+++ b/src/features/AgentTopicManager/TopicCard.tsx
@@ -11,6 +11,7 @@ import { useTranslation } from 'react-i18next';
 import { useWorkspaceAwareNavigate } from '@/features/Workspace/useWorkspaceAwareNavigate';
 import { useActivityTime } from '@/hooks/useActivityTime';
 import type { ChatTopic } from '@/types/topic';
+import { isModifierClick } from '@/utils/navigation';
 
 import StatusDot from './StatusDot';
 import { useTopicsViewStore } from './store';
@@ -105,6 +106,8 @@ const TopicCard = memo<TopicCardProps>(({ topic, agentId }) => {
     [selectMode, topic.id, agentId, toggleSelected, navigate],
   );
 
+  const href = useMemo(() => AGENT_CHAT_TOPIC_URL(agentId, topic.id), [agentId, topic.id]);
+
   const handleCheckboxChange = useCallback(() => {
     if (!selectMode) toggleSelectMode();
     toggleSelected(topic.id);
@@ -155,7 +158,22 @@ const TopicCard = memo<TopicCardProps>(({ topic, agentId }) => {
           <Icon icon={Star} size={13} style={{ color: cssVar.colorWarning, flexShrink: 0 }} />
         )}
         <Text className={styles.title} fontSize={14} weight={600}>
-          {topic.title || t('defaultTitle')}
+          <a
+            href={href}
+            style={{ color: 'inherit' }}
+            target="_blank"
+            onClick={(e) => {
+              if (isModifierClick(e)) {
+                // Allow only modifier click (Cmd+Click on Mac, Ctrl+Click on other OS)
+                // and manual navigation via context menu
+                e.stopPropagation();
+              } else {
+                e.preventDefault();
+              }
+            }}
+          >
+            {topic.title || t('defaultTitle')}
+          </a>
         </Text>
       </Flexbox>
 
diff --git a/src/routes/(main)/agent/_layout/Sidebar/Topic/List/Item/index.tsx b/src/routes/(main)/agent/_layout/Sidebar/Topic/List/Item/index.tsx
index c1ec27cea4..159a61fa54 100644
--- a/src/routes/(main)/agent/_layout/Sidebar/Topic/List/Item/index.tsx
+++ b/src/routes/(main)/agent/_layout/Sidebar/Topic/List/Item/index.tsx
@@ -9,7 +9,7 @@ import { Flexbox, Icon, Popover, Skeleton, Tag, Text, Tooltip } from '@lobehub/u
 import { createStaticStyles, cssVar, useTheme } from 'antd-style';
 import dayjs from 'dayjs';
 import { HashIcon, MessageSquareDashed } from 'lucide-react';
-import type { CSSProperties, DragEvent } from 'react';
+import type { CSSProperties, DragEvent, MouseEvent } from 'react';
 import { memo, Suspense, useCallback, useEffect, useMemo, useState } from 'react';
 import { useTranslation } from 'react-i18next';
 
@@ -33,6 +33,7 @@ import { useChatStore } from '@/store/chat';
 import { operationSelectors } from '@/store/chat/selectors';
 import { messageMapKey } from '@/store/chat/utils/messageMapKey';
 import { useElectronStore } from '@/store/electron';
+import { isModifierClick } from '@/utils/navigation';
 
 import { useTopicNavigation } from '../../hooks/useTopicNavigation';
 import ThreadList from '../../TopicListContent/ThreadList';
@@ -235,18 +236,22 @@ const TopicItem = memo<TopicItemProps>(
       [id],
     );
 
-    const handleClick = useCallback(() => {
-      if (editing) return;
-      if (isDesktop) {
-        cancelPendingSingleClick();
-        pendingSingleClickTimer = setTimeout(() => {
-          pendingSingleClickTimer = null;
+    const handleClick = useCallback(
+      (e: MouseEvent) => {
+        if (editing) return;
+        if (isModifierClick(e)) return;
+        if (isDesktop) {
+          cancelPendingSingleClick();
+          pendingSingleClickTimer = setTimeout(() => {
+            pendingSingleClickTimer = null;
+            void navigateToTopic(id);
+          }, 250);
+        } else {
           void navigateToTopic(id);
-        }, 250);
-      } else {
-        void navigateToTopic(id);
-      }
-    }, [editing, id, navigateToTopic]);
+        }
+      },
+      [editing, id, navigateToTopic],
+    );
 
     const handleDoubleClick = useCallback(async () => {
       if (!id || !activeAgentId || !isDesktop) return;
-- 
2.39.5


From ad5453886f8ab265a20a16058e59e327b79de029 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Mon, 6 Jul 2026 18:48:42 +0000
Subject: [PATCH 25/28] Do not add additives to system prompt

---
 .../builtin-agents/src/agents/inbox/index.ts     |  3 +--
 .../src/engine/messages/MessagesEngine.ts        | 16 +---------------
 src/services/chat/mecha/agentConfigResolver.ts   |  4 +---
 3 files changed, 3 insertions(+), 20 deletions(-)

diff --git a/packages/builtin-agents/src/agents/inbox/index.ts b/packages/builtin-agents/src/agents/inbox/index.ts
index 319c098120..397d86058b 100644
--- a/packages/builtin-agents/src/agents/inbox/index.ts
+++ b/packages/builtin-agents/src/agents/inbox/index.ts
@@ -3,7 +3,6 @@ import { UserInteractionIdentifier } from '@lobechat/builtin-tool-user-interacti
 
 import type { BuiltinAgentDefinition } from '../../types';
 import { BUILTIN_AGENT_SLUGS } from '../../types';
-import { createSystemRole } from './systemRole';
 
 /**
  * Inbox Agent - the default assistant agent for general conversations
@@ -14,7 +13,7 @@ export const INBOX: BuiltinAgentDefinition = {
   avatar: '/avatars/lobe-ai.png',
   runtime: (ctx) => ({
     plugins: [AgentDocumentsIdentifier, UserInteractionIdentifier, ...(ctx.plugins || [])],
-    systemRole: createSystemRole(ctx.userLocale),
+    systemRole: '',
   }),
 
   slug: BUILTIN_AGENT_SLUGS.inbox,
diff --git a/packages/context-engine/src/engine/messages/MessagesEngine.ts b/packages/context-engine/src/engine/messages/MessagesEngine.ts
index f95f5e133e..126597c1d0 100644
--- a/packages/context-engine/src/engine/messages/MessagesEngine.ts
+++ b/packages/context-engine/src/engine/messages/MessagesEngine.ts
@@ -1,4 +1,4 @@
-import debug from 'debug';
+import debug, { enabled } from 'debug';
 
 import type { OpenAIChatMessage } from '@/types/index';
 
@@ -43,7 +43,6 @@ import {
   HistorySummaryProvider,
   KnowledgeInjector,
   LocalSystemToolSnapshotInjector,
-  ModelInfoProvider,
   OnboardingActionHintInjector,
   OnboardingContextInjector,
   OnboardingSyntheticStateInjector,
@@ -52,7 +51,6 @@ import {
   PlanInjector,
   SelectedSkillInjector,
   SkillContextProvider,
-  SystemDateProvider,
   SystemRoleInjector,
   TaskManagerContextInjector,
   TodoInjector,
@@ -246,18 +244,6 @@ export class MessagesEngine {
         context: botPlatformContext,
         enabled: !!botPlatformContext,
       }),
-      // System date
-      new SystemDateProvider({ enabled: isSystemDateEnabled, timezone }),
-      // Model info (name / id / knowledge cutoff)
-      new ModelInfoProvider({
-        displayName: modelDisplayName,
-        knowledgeCutoff: modelKnowledgeCutoff,
-        modelId: model,
-        nativeMediaCapabilities: {
-          video: capabilities?.isCanUseVideo?.(model, provider),
-          vision: capabilities?.isCanUseVision?.(model, provider),
-        },
-      }),
       // Skill context (available skills list + activated skill content).
       // Disabled in chat mode — pairs with the tools-engine gate so the LLM
       // sees neither the manifests nor the discovery prompt.
diff --git a/src/services/chat/mecha/agentConfigResolver.ts b/src/services/chat/mecha/agentConfigResolver.ts
index 37f6da1e9f..c70af8d189 100644
--- a/src/services/chat/mecha/agentConfigResolver.ts
+++ b/src/services/chat/mecha/agentConfigResolver.ts
@@ -265,9 +265,7 @@ export const resolveAgentConfig = (ctx: AgentConfigResolverContext): ResolvedAge
     const userLocale = userGeneralSettingsSelectors.currentResponseLanguage(
       useUserStore.getState(),
     );
-    const localeInstruction = userLocale
-      ? `Preferred reply language: ${userLocale}. Use this language unless the user explicitly asks to switch.`
-      : '';
+    const localeInstruction = '';
     const systemRoleWithLocale = localeInstruction
       ? agentConfig.systemRole
         ? `${agentConfig.systemRole}\n\n${localeInstruction}`
-- 
2.39.5


From 54148008d31a0d4936ffc0304432d2a64f74362d Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Sun, 26 Jul 2026 09:14:36 +0000
Subject: [PATCH 26/28] Wait @lobehub/editor@4.23.x for a week

Due to minimumReleaseAge
---
 package.json | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/package.json b/package.json
index 7793a909a5..7f18094274 100644
--- a/package.json
+++ b/package.json
@@ -295,7 +295,7 @@
     "@lobehub/analytics": "^1.6.3",
     "@lobehub/charts": "^5.4.0",
     "@lobehub/desktop-ipc-typings": "workspace:*",
-    "@lobehub/editor": "^4.19.1",
+    "@lobehub/editor": "^4.19.1 <4.23.0",
     "@lobehub/icons": "^5.11.0",
     "@lobehub/market-sdk": "0.39.2",
     "@lobehub/tts": "^5.1.2",
-- 
2.39.5


From 81d4189fc4ddb7733f33d38145df625e6ae6a735 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Sun, 26 Jul 2026 08:13:14 +0000
Subject: [PATCH 27/28] Fix 900s timeout issue

---
 packages/model-runtime/package.json           |  1 +
 .../src/core/openaiCompatibleFactory/index.ts | 29 ++++++-
 .../core/openaiCompatibleFactory/timeout.ts   | 75 +++++++++++++++++++
 .../(backend)/webapi/chat/[provider]/route.ts |  2 +-
 src/instrumentation.ts                        | 23 ++++++
 src/libs/next/config/define-config.ts         |  1 +
 6 files changed, 129 insertions(+), 2 deletions(-)
 create mode 100644 packages/model-runtime/src/core/openaiCompatibleFactory/timeout.ts

diff --git a/packages/model-runtime/package.json b/packages/model-runtime/package.json
index a70b2cfd50..47770af704 100644
--- a/packages/model-runtime/package.json
+++ b/packages/model-runtime/package.json
@@ -37,6 +37,7 @@
     "replicate": "^1.4.0",
     "tokenx": "^1.3.0",
     "type-fest": "^5.7.0",
+    "undici": "^7.28.0",
     "url-join": "^5.0.0"
   },
   "devDependencies": {
diff --git a/packages/model-runtime/src/core/openaiCompatibleFactory/index.ts b/packages/model-runtime/src/core/openaiCompatibleFactory/index.ts
index b0c51e3304..0380d8eaba 100644
--- a/packages/model-runtime/src/core/openaiCompatibleFactory/index.ts
+++ b/packages/model-runtime/src/core/openaiCompatibleFactory/index.ts
@@ -72,10 +72,15 @@ import { convertOpenAIResponseUsage, convertOpenAIUsage } from '../usageConverte
 import { createOpenAICompatibleImage } from './createImage';
 import { createOpenAICompatibleVideo, pollOpenAICompatibleVideoStatus } from './createVideo';
 import { transformResponseAPIToStream, transformResponseToStream } from './nonStreamToStream';
+import {
+  DEFAULT_OPENAI_TIMEOUT,
+  resolveOpenAIClientNetworkOptions,
+} from './timeout';
 
 export type { PollVideoStatusResult };
 export * from './createVideo';
 export * from './nonStreamToStream';
+export * from './timeout';
 
 /**
  * Detect provider 400/422 errors that reject `response_format: { type: 'json_schema' }`.
@@ -356,7 +361,29 @@ export const createOpenAICompatibleRuntime = <T extends Record<string, any> = an
 
       if (!apiKey) throw AgentRuntimeError.createError(ErrorType?.invalidAPIKey);
 
-      const initOptions = { apiKey, baseURL, ...constructorOptions, ...res };
+      const explicitTimeout =
+        (typeof res.timeout === 'number' ? res.timeout : undefined) ??
+        (typeof constructorOptions?.timeout === 'number' ? constructorOptions.timeout : undefined);
+      const resolvedTimeout = explicitTimeout ?? DEFAULT_OPENAI_TIMEOUT;
+      const networkOptions = resolveOpenAIClientNetworkOptions(resolvedTimeout);
+
+      const initOptions: LobeClientOptions & Record<string, any> = {
+        apiKey,
+        baseURL,
+        ...constructorOptions,
+        ...res,
+        timeout: resolvedTimeout,
+      };
+
+      // Align undici headers/body timeouts with the SDK abort timer. Skip when the
+      // caller already supplied a custom fetch (must stay on the same undici instance).
+      if (!initOptions.fetch && networkOptions.fetch) {
+        initOptions.fetch = networkOptions.fetch;
+        initOptions.fetchOptions = {
+          ...(initOptions.fetchOptions as object | undefined),
+          ...networkOptions.fetchOptions,
+        };
+      }
 
       // if the custom client is provided, use it as client
       if (customClient?.createClient) {
diff --git a/packages/model-runtime/src/core/openaiCompatibleFactory/timeout.ts b/packages/model-runtime/src/core/openaiCompatibleFactory/timeout.ts
new file mode 100644
index 0000000000..9b52bd92d9
--- /dev/null
+++ b/packages/model-runtime/src/core/openaiCompatibleFactory/timeout.ts
@@ -0,0 +1,75 @@
+export const DEFAULT_OPENAI_TIMEOUT = 3_600_000;
+
+export interface OpenAIClientNetworkOptions {
+  fetch?: typeof fetch;
+  fetchOptions?: { dispatcher: unknown };
+  timeout: number;
+}
+
+type UndiciModule = {
+  Agent: new (options?: { bodyTimeout?: number; headersTimeout?: number }) => unknown;
+  fetch: typeof fetch;
+};
+
+/**
+ * Prefer the undici instance installed by `src/instrumentation.ts` (same package
+ * as the global dispatcher). Fall back to a hidden require for non-Next runners.
+ */
+const loadUndici = (): UndiciModule | undefined => {
+  if (typeof window !== 'undefined') return undefined;
+  if (typeof process === 'undefined' || !process.versions?.node) return undefined;
+
+  const fromInstrumentation = (globalThis as typeof globalThis & { __LOBE_UNDICI__?: UndiciModule })
+    .__LOBE_UNDICI__;
+  if (fromInstrumentation?.Agent && fromInstrumentation.fetch) {
+    return fromInstrumentation;
+  }
+
+  try {
+    // Hide from Vite; works when the module graph is CJS.
+    const nodeRequire = eval('require') as NodeRequire;
+    return nodeRequire('undici') as UndiciModule;
+  } catch {
+    // ESM / Next: require is undefined
+  }
+
+  try {
+    const nodeModule = process.getBuiltinModule?.('module') as
+      { createRequire: (filename: string) => NodeRequire } | undefined;
+    if (!nodeModule?.createRequire) return undefined;
+    return nodeModule.createRequire(`${process.cwd()}/package.json`)('undici') as UndiciModule;
+  } catch {
+    return undefined;
+  }
+};
+
+/**
+ * Align OpenAI SDK abort timer with undici headers/body idle timeouts.
+ * Without a matching Agent on the same fetch implementation, Node's default
+ * undici agent still aborts at 300s (× SDK retries ≈ 900s).
+ */
+export const resolveOpenAIClientNetworkOptions = (
+  timeoutMs: number = DEFAULT_OPENAI_TIMEOUT,
+): OpenAIClientNetworkOptions => {
+  const timeout = Number.isFinite(timeoutMs) && timeoutMs >= 0 ? timeoutMs : DEFAULT_OPENAI_TIMEOUT;
+
+  const undici = loadUndici();
+  if (!undici?.Agent || !undici.fetch) {
+    console.warn(
+      '[model-runtime] undici Agent unavailable — relying on instrumentation global fetch if present. timeoutMs=%s',
+      timeout,
+    );
+    return { timeout };
+  }
+
+  const dispatcher = new undici.Agent({
+    bodyTimeout: timeout,
+    headersTimeout: timeout,
+  });
+
+  return {
+    fetch: undici.fetch as unknown as typeof fetch,
+    fetchOptions: { dispatcher },
+    timeout,
+  };
+};
diff --git a/src/app/(backend)/webapi/chat/[provider]/route.ts b/src/app/(backend)/webapi/chat/[provider]/route.ts
index 4a6118f8b2..8e09ffe450 100644
--- a/src/app/(backend)/webapi/chat/[provider]/route.ts
+++ b/src/app/(backend)/webapi/chat/[provider]/route.ts
@@ -12,7 +12,7 @@ import { resolveValidWorkspaceIdFromRequest } from '../../_utils/workspace';
 
 // If user don't use fluid compute, will build  failed
 // this enforce user to enable fluid compute
-export const maxDuration = 300;
+export const maxDuration = 3600;
 
 export const POST = checkAuth(async (req: Request, { params, userId, serverDB }) => {
   const provider = (await params)!.provider!;
diff --git a/src/instrumentation.ts b/src/instrumentation.ts
index 1223e7eec4..131c62f093 100644
--- a/src/instrumentation.ts
+++ b/src/instrumentation.ts
@@ -1,4 +1,27 @@
 export async function register() {
+  // Node's built-in fetch uses undici defaults (headersTimeout/bodyTimeout
+  // 300s). OpenAI SDK retries twice → ~900s "Request timed out." / ProviderBizError.
+  // Swap in the undici package fetch + a 1h dispatcher so long chat TTFT/streams work.
+  if (process.env.NEXT_RUNTIME === 'nodejs') {
+    try {
+      const undici = await import('undici');
+      const timeoutMs = 3_600_000;
+      undici.setGlobalDispatcher(
+        new undici.Agent({
+          bodyTimeout: timeoutMs,
+          headersTimeout: timeoutMs,
+        }),
+      );
+      // OpenAI's client uses global fetch unless given a custom one; binding the
+      // same undici instance as the dispatcher is required for timeouts to apply.
+      globalThis.fetch = undici.fetch as unknown as typeof fetch;
+      (globalThis as typeof globalThis & { __LOBE_UNDICI__?: typeof undici }).__LOBE_UNDICI__ =
+        undici;
+    } catch (error) {
+      console.warn('[Instrumentation] failed to install undici fetch+dispatcher:', error);
+    }
+  }
+
   // In local development, write debug logs to logs/server.log
   if (process.env.NODE_ENV !== 'production' && process.env.NEXT_RUNTIME === 'nodejs') {
     await import('./libs/debug-file-logger');
diff --git a/src/libs/next/config/define-config.ts b/src/libs/next/config/define-config.ts
index f420130d17..9a71ad655e 100644
--- a/src/libs/next/config/define-config.ts
+++ b/src/libs/next/config/define-config.ts
@@ -364,6 +364,7 @@ export function defineConfig(config: CustomNextConfig) {
       'pdfjs-dist',
       'ajv',
       'oidc-provider',
+      'undici',
     ],
 
     transpilePackages: ['mermaid'],
-- 
2.39.5


From 6fac9951401598aac82dec3118668a878e604944 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Mon, 27 Jul 2026 03:41:09 +0000
Subject: [PATCH 28/28] Disable retries (OpenAI, LobeChat)

---
 .../src/modules/AgentRuntime/adapters/ServerLLMTransport.ts     | 1 +
 .../model-runtime/src/core/openaiCompatibleFactory/index.ts     | 2 ++
 src/store/chat/agents/transports/ClientLLMTransport.ts          | 1 +
 3 files changed, 4 insertions(+)

diff --git a/apps/server/src/modules/AgentRuntime/adapters/ServerLLMTransport.ts b/apps/server/src/modules/AgentRuntime/adapters/ServerLLMTransport.ts
index a20272320b..0b38c6f5a4 100644
--- a/apps/server/src/modules/AgentRuntime/adapters/ServerLLMTransport.ts
+++ b/apps/server/src/modules/AgentRuntime/adapters/ServerLLMTransport.ts
@@ -50,6 +50,7 @@ const getErrorMessage = (error: unknown): string => {
 };
 
 const SERVER_LLM_RETRY_POLICY = {
+  maxRetries: 0,
   noRetryProviders: [BRANDING_PROVIDER],
 };
 
diff --git a/packages/model-runtime/src/core/openaiCompatibleFactory/index.ts b/packages/model-runtime/src/core/openaiCompatibleFactory/index.ts
index 0380d8eaba..ddf2b22387 100644
--- a/packages/model-runtime/src/core/openaiCompatibleFactory/index.ts
+++ b/packages/model-runtime/src/core/openaiCompatibleFactory/index.ts
@@ -372,6 +372,8 @@ export const createOpenAICompatibleRuntime = <T extends Record<string, any> = an
         baseURL,
         ...constructorOptions,
         ...res,
+        // Self-host: never auto-retry upstream — timeouts/5xx would re-bill the same prompt.
+        maxRetries: 0,
         timeout: resolvedTimeout,
       };
 
diff --git a/src/store/chat/agents/transports/ClientLLMTransport.ts b/src/store/chat/agents/transports/ClientLLMTransport.ts
index c797efff63..2e965a702a 100644
--- a/src/store/chat/agents/transports/ClientLLMTransport.ts
+++ b/src/store/chat/agents/transports/ClientLLMTransport.ts
@@ -36,6 +36,7 @@ import type { ClientLLMModelParameters } from './ClientContextBuilder';
 import type { ClientRuntimeSession } from './ClientRuntimeStreamSink';
 
 const CLIENT_LLM_RETRY_POLICY = {
+  maxRetries: 0,
   noRetryProviders: [BRANDING_PROVIDER],
 };
 
-- 
2.39.5

