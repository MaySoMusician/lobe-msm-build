From 01edd88e00c864b0a792c9ece116758b5b7639ac Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Thu, 6 Feb 2025 20:34:05 +0000
Subject: [PATCH 1/8] Load Google Fonts script and IBM Plex Sans JP

---
 src/app/[variants]/layout.tsx | 7 +++++++
 1 file changed, 7 insertions(+)

diff --git a/src/app/[variants]/layout.tsx b/src/app/[variants]/layout.tsx
index 2a4b01642..6cf933390 100644
--- a/src/app/[variants]/layout.tsx
+++ b/src/app/[variants]/layout.tsx
@@ -37,6 +37,13 @@ const RootLayout = async ({ children, params, modal }: RootLayoutProps) => {
           // eslint-disable-next-line @next/next/no-sync-scripts
           <script crossOrigin="anonymous" src="https://unpkg.com/react-scan/dist/auto.global.js" />
         )}
+        <link href="https://fonts.googleapis.com" rel="preconnect" />
+        <link crossOrigin="anonymous" href="https://fonts.gstatic.com" rel="preconnect" />
+        {/* eslint-disable-next-line @next/next/no-page-custom-font */}
+        <link
+          href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+JP:wght@400;500;600;700&display=swap"
+          rel="stylesheet"
+        />
       </head>
       <body>
         <NuqsAdapter>
-- 
2.39.5


From ec6df82669b220d0beb7e2700fb0fe964659886a Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Fri, 7 Feb 2025 08:18:42 +0000
Subject: [PATCH 2/8] Force to use en-US as language

---
 src/utils/locale.ts | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/src/utils/locale.ts b/src/utils/locale.ts
index 0fe495d86..608a9a50a 100644
--- a/src/utils/locale.ts
+++ b/src/utils/locale.ts
@@ -43,7 +43,7 @@ export const parseBrowserLanguage = (headers: Headers, defaultLang: string = DEF
   // if match the ar-EG then fallback to ar
   if (browserLang === 'ar-EG') browserLang = 'ar';
 
-  return browserLang;
+  return 'en-US';
 };
 
 /**
-- 
2.39.5


From c66c30e99e6ee42408194404dc7579d37f18fe8e Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Fri, 7 Feb 2025 08:28:43 +0000
Subject: [PATCH 3/8] Show only English and Japanese option for language
 options in global menu

---
 src/features/User/UserPanel/LangButton.tsx | 12 +++++++-----
 1 file changed, 7 insertions(+), 5 deletions(-)

diff --git a/src/features/User/UserPanel/LangButton.tsx b/src/features/User/UserPanel/LangButton.tsx
index 3b625746e..b0870dfde 100644
--- a/src/features/User/UserPanel/LangButton.tsx
+++ b/src/features/User/UserPanel/LangButton.tsx
@@ -29,11 +29,13 @@ const LangButton = memo<{ placement?: PopoverProps['placement'] }>(({ placement
         label: t('settingCommon.lang.autoMode'),
         onClick: () => handleLangChange('auto'),
       },
-      ...localeOptions.map((item) => ({
-        key: item.value,
-        label: item.label,
-        onClick: () => handleLangChange(item.value),
-      })),
+      ...localeOptions
+        .filter((item) => ['en-US', 'ja-JP'].includes(item.value))
+        .map((item) => ({
+          key: item.value,
+          label: item.label,
+          onClick: () => handleLangChange(item.value),
+        })),
     ],
     [t],
   );
-- 
2.39.5


From 86e1846d97938c6c6c941c7434934e8abe9ad88e Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Mon, 19 May 2025 05:51:10 +0000
Subject: [PATCH 4/8] Use custom summaryTitle prompt written in English

---
 src/chains/summaryTitle.ts | 10 ++++++----
 1 file changed, 6 insertions(+), 4 deletions(-)

diff --git a/src/chains/summaryTitle.ts b/src/chains/summaryTitle.ts
index 4e25cfb44..ca73e7b9b 100644
--- a/src/chains/summaryTitle.ts
+++ b/src/chains/summaryTitle.ts
@@ -2,18 +2,20 @@ import { globalHelpers } from '@/store/global/helpers';
 import { ChatStreamPayload, OpenAIChatMessage } from '@/types/openai/chat';
 
 export const chainSummaryTitle = (messages: OpenAIChatMessage[]): Partial<ChatStreamPayload> => {
-  const lang = globalHelpers.getCurrentLanguage();
+  globalHelpers.getCurrentLanguage();
 
   return {
     messages: [
       {
-        content: '你是一名擅长会话的助理，你需要将用户的会话总结为 10 个字以内的标题',
+        content:
+          'You are an assistant proficient in conversation. Your task is to summarize the user’s conversation into a short title. Read the conversation and follow `@@@Instructions@@@`.',
         role: 'system',
       },
       {
-        content: `${messages.map((message) => `${message.role}: ${message.content}`).join('\n')}
+        content: `${messages.map((message) => `[${message.role}]\n${message.content}`).join('\n\n')}
 
-请总结上述对话为10个字以内的标题，不需要包含标点符号，输出语言语种为：${lang}`,
+@@@Instructions@@@
+Please summarize the above conversation as a title in the same language used within it. If the conversation is in a European language (e.g., English), the title should be within around 5 words and omit any punctuation. If the conversation is in a CJK language (e.g., Japanese), the title should be within around 10 characters and also omit any punctuation.`,
         role: 'user',
       },
     ],
-- 
2.39.5


From 62915cdbc403a30c6ee2698d12fb8ae65210301f Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Tue, 1 Jul 2025 18:35:39 +0000
Subject: [PATCH 5/8] Overwrite reasoning.summary to undefined for requests for
 openai

---
 packages/model-runtime/src/openai/index.ts | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/packages/model-runtime/src/openai/index.ts b/packages/model-runtime/src/openai/index.ts
index 2fc6dbada..4eab6c182 100644
--- a/packages/model-runtime/src/openai/index.ts
+++ b/packages/model-runtime/src/openai/index.ts
@@ -79,8 +79,8 @@ export const LobeOpenAI = createOpenAICompatibleRuntime({
           ...rest,
           model,
           reasoning: payload.reasoning
-            ? { ...payload.reasoning, summary: 'auto' }
-            : { summary: 'auto' },
+            ? { ...payload.reasoning, summary: undefined }
+            : { summary: undefined },
           stream: payload.stream ?? true,
           tools: openaiTools as any,
           // computer-use series must set truncation as auto
-- 
2.39.5


From ded8c5aca6dfdd02fa5e2acf7cc47fb2ab256778 Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Sun, 10 Aug 2025 05:01:38 +0000
Subject: [PATCH 6/8] Mark page and components in main as force-dynamic

cf. https://github.com/lobehub/lobe-chat/issues/8732#issuecomment-3171917869
---
 src/app/[variants]/(main)/chat/(workspace)/page.tsx | 2 ++
 src/app/[variants]/(main)/chat/settings/page.tsx    | 2 ++
 2 files changed, 4 insertions(+)

diff --git a/src/app/[variants]/(main)/chat/(workspace)/page.tsx b/src/app/[variants]/(main)/chat/(workspace)/page.tsx
index 132fd8b8f..883df5c43 100644
--- a/src/app/[variants]/(main)/chat/(workspace)/page.tsx
+++ b/src/app/[variants]/(main)/chat/(workspace)/page.tsx
@@ -14,6 +14,8 @@ import PageTitle from '../features/PageTitle';
 import Changelog from './features/ChangelogModal';
 import TelemetryNotification from './features/TelemetryNotification';
 
+export const dynamic = 'force-dynamic';
+
 export const generateMetadata = async (props: DynamicLayoutProps) => {
   const locale = await RouteVariants.getLocale(props);
   const { t } = await translation('metadata', locale);
diff --git a/src/app/[variants]/(main)/chat/settings/page.tsx b/src/app/[variants]/(main)/chat/settings/page.tsx
index 884df6f91..df085893e 100644
--- a/src/app/[variants]/(main)/chat/settings/page.tsx
+++ b/src/app/[variants]/(main)/chat/settings/page.tsx
@@ -16,6 +16,8 @@ import { ChatSettingsTabs } from '@/store/global/initialState';
 import { useSessionStore } from '@/store/session';
 import { sessionMetaSelectors } from '@/store/session/selectors';
 
+export const dynamic = 'force-dynamic';
+
 const EditPage = memo(() => {
   const { t } = useTranslation('setting');
   const [tab, setTab] = useState(ChatSettingsTabs.Prompt);
-- 
2.39.5


From 2ad251ffd48ea246593bd5fc79ae45cbf733782a Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Sun, 10 Aug 2025 05:03:35 +0000
Subject: [PATCH 7/8] Disable stream for gpt-5 for unverified organizations

---
 packages/const/src/models.ts | 4 ++++
 1 file changed, 4 insertions(+)

diff --git a/packages/const/src/models.ts b/packages/const/src/models.ts
index 3aed6761e..5393692ad 100644
--- a/packages/const/src/models.ts
+++ b/packages/const/src/models.ts
@@ -18,6 +18,10 @@ export const disableStreamModels = new Set([
   */
   'computer-use-preview',
   'computer-use-preview-2025-03-11',
+  'gpt-5',
+  'gpt-5-2025-08-07',
+  'gpt-5-mini',
+  'gpt-5-mini-2025-08-07',
 ]);
 
 /**
-- 
2.39.5


From f64793dc422f00f92458939b0bd4e7321c347c4c Mon Sep 17 00:00:00 2001
From: MaySoMusician <maysomusician@gmail.com>
Date: Sun, 10 Aug 2025 05:43:06 +0000
Subject: [PATCH 8/8] Implement faking response stream

---
 .../utils/openaiCompatibleFactory/index.ts    |  41 ++--
 .../openaiCompatibleFactory/transform2.ts     | 220 ++++++++++++++++++
 2 files changed, 248 insertions(+), 13 deletions(-)
 create mode 100644 packages/model-runtime/src/utils/openaiCompatibleFactory/transform2.ts

diff --git a/packages/model-runtime/src/utils/openaiCompatibleFactory/index.ts b/packages/model-runtime/src/utils/openaiCompatibleFactory/index.ts
index 509dd9ee6..b5dbdd774 100644
--- a/packages/model-runtime/src/utils/openaiCompatibleFactory/index.ts
+++ b/packages/model-runtime/src/utils/openaiCompatibleFactory/index.ts
@@ -37,6 +37,7 @@ import {
 } from '../openaiHelpers';
 import { StreamingResponse } from '../response';
 import { OpenAIResponsesStream, OpenAIStream, OpenAIStreamOptions } from '../streams';
+import { transformNonStreamingResponseToStream } from './transform2';
 
 // the model contains the following keywords is not a chat model, so we should filter them out
 export const CHAT_MODELS_BLOCK_LIST = [
@@ -643,7 +644,9 @@ export const createOpenAICompatibleRuntime = <T extends Record<string, any> = an
         input,
         store: false,
         tools: tools?.map((tool) => this.convertChatCompletionToolToResponseTool(tool)),
-      } as OpenAI.Responses.ResponseCreateParamsStreaming;
+      } as
+        | OpenAI.Responses.ResponseCreateParamsStreaming
+        | OpenAI.Responses.ResponseCreateParamsNonStreaming;
 
       if (debug?.responses?.()) {
         console.log('[requestPayload]');
@@ -655,24 +658,36 @@ export const createOpenAICompatibleRuntime = <T extends Record<string, any> = an
         signal: options?.signal,
       });
 
-      const [prod, useForDebug] = response.tee();
-
-      if (debug?.responses?.()) {
-        const useForDebugStream =
-          useForDebug instanceof ReadableStream ? useForDebug : useForDebug.toReadableStream();
-
-        debugStream(useForDebugStream).catch(console.error);
-      }
-
       const streamOptions: OpenAIStreamOptions = {
         bizErrorTypeTransformer: chatCompletion?.handleStreamBizErrorType,
         callbacks: options?.callback,
         provider: this.id,
       };
 
-      return StreamingResponse(OpenAIResponsesStream(prod, { ...streamOptions, inputStartAt }), {
-        headers: options?.headers,
-      });
+      if ('tee' in response) {
+        const [prod, useForDebug] = response.tee();
+
+        if (debug?.responses?.()) {
+          const useForDebugStream =
+            useForDebug instanceof ReadableStream ? useForDebug : useForDebug.toReadableStream();
+
+          debugStream(useForDebugStream).catch(console.error);
+        }
+
+        return StreamingResponse(OpenAIResponsesStream(prod, { ...streamOptions, inputStartAt }), {
+          headers: options?.headers,
+        });
+      }
+
+      return StreamingResponse(
+        OpenAIResponsesStream(transformNonStreamingResponseToStream(response), {
+          ...streamOptions,
+          inputStartAt,
+        }),
+        {
+          headers: options?.headers,
+        },
+      );
     }
 
     private convertChatCompletionToolToResponseTool = (tool: ChatCompletionTool) => {
diff --git a/packages/model-runtime/src/utils/openaiCompatibleFactory/transform2.ts b/packages/model-runtime/src/utils/openaiCompatibleFactory/transform2.ts
new file mode 100644
index 000000000..8247dd243
--- /dev/null
+++ b/packages/model-runtime/src/utils/openaiCompatibleFactory/transform2.ts
@@ -0,0 +1,220 @@
+import OpenAI from 'openai';
+
+function convertNonStreamingResponseToStreamEvents(
+  nonStreamingResponse: OpenAI.Responses.Response,
+): OpenAI.Responses.ResponseStreamEvent[] {
+  const events: OpenAI.Responses.ResponseStreamEvent[] = [];
+  let seq = 1;
+
+  // 1. First, create the "response.created" event.
+  events.push({
+    response: {
+      created_at: 0, // dummy placeholder
+      error: null, // dummy placeholder
+      id: nonStreamingResponse.id,
+      incomplete_details: null, // dummy placeholder
+      instructions: null, // dummy placeholder
+      metadata: null, // dummy placeholder
+      model: '', // dummy placeholder
+      object: 'response', // dummy placeholder
+      output: [], // dummy placeholder
+      output_text: '', // dummy placeholder
+      parallel_tool_calls: false, // dummy placeholder
+      status:
+        nonStreamingResponse.status === 'completed' ? 'in_progress' : nonStreamingResponse.status,
+      temperature: null, // dummy placeholder
+      tool_choice: 'auto', // dummy placeholder
+      tools: [], // dummy placeholder
+      top_p: null, // dummy placeholder
+      usage: undefined,
+    },
+    sequence_number: seq++,
+    type: 'response.created',
+  });
+
+  // 2. Traverse the output array and generate events according to each item's type.
+  for (const item of nonStreamingResponse.output) {
+    // Send the "response.output_item.added" event first.
+    events.push({
+      item: {
+        arguments: (item as any).arguments,
+        call_id: (item as any).call_id,
+        content: (item as any).content,
+        id: item.id,
+        name: (item as any).name,
+        role: (item as any).role,
+        status: (item as any).status,
+        summary: (item as any).summary,
+        type: item.type,
+      },
+      sequence_number: seq++,
+      type: 'response.output_item.added',
+    } as any);
+
+    // Branch processing according to item.type.
+    switch (item.type) {
+      case 'function_call': {
+        // In src/utils/fetch/fetchSSE.ts, the argument duplication occurs during parse, so we won't send a delta.
+        events.push({
+          item: {
+            arguments: item.arguments, // dummy placeholder
+            call_id: item.call_id,
+            id: item.id,
+            name: '', // dummy placeholder
+            type: 'function_call',
+          },
+          output_index: 1,
+          sequence_number: seq++,
+          type: 'response.output_item.done',
+        });
+        break;
+      }
+
+      case 'message': {
+        // For "message" type, send the entire content as a single "response.output_text.delta" event.
+        if (item.content) {
+          for (const contentObj of item.content) {
+            if (contentObj.type === 'output_text') {
+              // Combine text into a single delta.
+              events.push({
+                content_index: 1, // dummy placeholder
+                delta: contentObj.text,
+                item_id: item.id,
+                output_index: 1, // dummy placeholder
+                sequence_number: seq++,
+                type: 'response.output_text.delta',
+              });
+
+              // If there are annotations, send them individually as "response.output_text.annotation.added" events.
+              if (Array.isArray(contentObj.annotations)) {
+                for (const ann of contentObj.annotations) {
+                  const ann2 = ann as OpenAI.Responses.ResponseOutputText.URLCitation;
+                  events.push({
+                    annotation: {
+                      end_index: ann2.end_index,
+                      start_index: ann2.start_index,
+                      title: ann2.title,
+                      type: 'url_citation',
+                      url: ann2.url,
+                    },
+                    item_id: item.id,
+                    sequence_number: seq++,
+                    type: 'response.output_text.annotation.added' as any,
+                  });
+                }
+              }
+            }
+          }
+        }
+
+        // Finally, send "response.output_item.done".
+        events.push({
+          item: {
+            content: [], // dummy placeholder
+            id: item.id,
+            role: 'assistant', // dummy placeholder
+            status: 'in_progress', // dummy placeholder
+            type: 'message',
+          },
+          output_index: 1, // dummy placeholder
+          sequence_number: seq++,
+          type: 'response.output_item.done',
+        });
+        break;
+      }
+
+      case 'reasoning': {
+        // For "reasoning" type, send the summary all at once.
+        // (In transformOpenAIStream, the events go in the order "response.reasoning_summary_part.added" -> "response.reasoning_summary_text.delta" -> done.)
+        events.push({
+          item_id: item.id,
+          output_index: 1, // dummy placeholder
+          part: {
+            text: '', // dummy placeholder
+            type: 'summary_text',
+          },
+          sequence_number: seq++,
+          summary_index: 1, // dummy placeholder
+          type: 'response.reasoning_summary_part.added',
+        });
+
+        // If there's a summary array, concatenate it into a single delta.
+        const entireSummary = Array.isArray(item.summary) ? item.summary.join('\n') : '';
+        if (entireSummary) {
+          events.push({
+            delta: entireSummary,
+            item_id: item.id,
+            output_index: 1, // dummy placeholder
+            sequence_number: seq++,
+            summary_index: 1, // dummy placeholder
+            type: 'response.reasoning_summary_text.delta',
+          });
+        }
+
+        events.push({
+          item: {
+            id: item.id,
+            summary: [], // dummy placeholder
+            type: 'reasoning',
+          },
+          output_index: 1, // dummy placeholder
+          sequence_number: seq++,
+          type: 'response.output_item.done',
+        });
+        break;
+      }
+
+      default: {
+        // For an unknown type, just send "response.output_item.done".
+        events.push({
+          item: {
+            id: item.id,
+            type: item.type,
+          },
+          sequence_number: seq++,
+          type: 'response.output_item.done',
+        } as any);
+        break;
+      }
+    }
+  }
+
+  // 3. Finally, send the "response.completed" event.
+  events.push({
+    response: {
+      created_at: 0, // dummy placeholder
+      error: null, // dummy placeholder
+      id: nonStreamingResponse.id,
+      incomplete_details: null, // dummy placeholder
+      instructions: null, // dummy placeholder
+      metadata: null, // dummy placeholder
+      model: '', // dummy placeholder
+      object: 'response', // dummy placeholder
+      output: [], // dummy placeholder
+      output_text: '', // dummy placeholder
+      parallel_tool_calls: false, // dummy placeholder
+      temperature: null, // dummy placeholder
+      tool_choice: 'auto', // dummy placeholder
+      tools: [], // dummy placeholder
+      top_p: null, // dummy placeholder
+      usage: nonStreamingResponse.usage ?? undefined,
+    },
+    sequence_number: seq++,
+    type: 'response.completed',
+  });
+
+  return events;
+}
+
+export function transformNonStreamingResponseToStream(data: OpenAI.Responses.Response) {
+  return new ReadableStream({
+    start(controller) {
+      const events = convertNonStreamingResponseToStreamEvents(data);
+
+      for (const event of events) {
+        controller.enqueue(event);
+      }
+      controller.close();
+    },
+  });
+}
-- 
2.39.5

